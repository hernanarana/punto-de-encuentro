// src/components/HeroBanner.jsx
export default function HeroBanner() {
  return (
    <section className="hero-banner only-desktop" aria-label="Destacados">
      {/* Contenido opcional: imagen, carrusel, CTA. Dejalo así si todavía no lo usás */}
      {/* <img src="/banners/hero.jpg" alt="Ofertas de la semana" /> */}
    </section>
  );
}
